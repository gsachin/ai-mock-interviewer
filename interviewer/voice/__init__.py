"""Voice pipeline (Phase 3). Everything here is skeleton until the LiveKit
integration lands; ``stubs`` provides a text-mode development path so the
interviewer brain (state machine + RAG + LLM) is testable without audio."""
